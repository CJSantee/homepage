beforeAll(() => {
  console.log('Before All');
});

afterAll(() => {
  console.log('After All');
});

describe('createNewPoolGame', () => {
  it('creates a new pool game', () => {
    expect(true).toBeTruthy();
  });
});